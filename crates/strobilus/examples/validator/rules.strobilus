on allow {
    if (principal.counter > 0) then {
        updateAttribute(principal.counter, "counter", principal.counter - 1);
    } else {
        updateAttribute(principal, "counter", 0);
    }
}
on deny { updateAttribute(principal, "counter", 0); }
